import { describe, it } from 'vitest';

describe('UserPage', () => {
  describe('Create', () => {
    it('should create the user when the Save button is clicked', () => {});
  });

  describe('Update', () => {
    it('should update the user when the Save button is clicked', () => {});
  });
});
